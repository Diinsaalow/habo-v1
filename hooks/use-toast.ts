// This file is referenced in components.json but not currently used in the application
// Keeping it for potential future use or to avoid build errors

export const useToast = () => {
  return {
    toast: () => {},
    dismiss: () => {},
  };
};
